depened copy (dpc)
2010.11.22


1.设定PATH环境变量，让denpend.bat和depend.sh可以在任意目录下运行
2.设定DEPEND_LIB_HOME环境变量，指向lib的本地目录
3.进入到项目的目录，depend.conf所在的目录下，执行depend.bat(Win)或depend.sh(Linux)


depend.conf需要预先编写；
项目需要拷贝的目的路径中，对应的目录需要预先创建，如project1/lib